require 'spec_helper'

describe Mygasfeed do
  it 'has a version number' do
    expect(Mygasfeed::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
